language: nb
responses:
  intents:
    HassTurnOn:
      default: "{{ slots.name }} ble slått på"
      lights_area: Lysene i {{ slots.area }} ble slått på
      fans_area: Viftene i {{ slots.area }} ble slått på
      cover: Åpnet {{ slots.name }}
      cover_area: Åpnet {{ slots.area }}
